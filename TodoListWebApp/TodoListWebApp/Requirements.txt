Create a web app to be able to enter and display todo items:

Todo should have Name and Description

The task is to add 2 new tabs in the page:
"Todo Items List" -> List all the todos
"Add new Todo" -> Allows adding new todo.

Advanced:
1. Validate thet non-empty todos should only be sent
2. Remove Privacy and Home tabs and web app initial load redirect to TodoList
3. Instead of DataService -> Write and read data from text file (Advanced)